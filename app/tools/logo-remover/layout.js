export const metadata = {
  title: 'Logo Remover - Erase Watermarks Online',
  description: 'Remove logos, watermarks, and unwanted objects from images manually. Free online eraser tool.',
  keywords: ['logo remover', 'watermark remover', 'erase objects', 'image eraser'],
};

export default function Layout({ children }) {
  return <>{children}</>;
}